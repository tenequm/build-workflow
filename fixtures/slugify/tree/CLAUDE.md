# textkit

Throwaway fixture for end-to-end verification of the build-workflow skills.
The build adds `slugify` to a package that starts with `word_count` only.

- Whole-tree check: `python3 -m unittest discover -s tests -q`
- Plans directory: `docs/plans/`
- Stdlib only, Python 3.13. No product spec, no release ceremony.
