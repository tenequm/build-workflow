# pond#237 and review-pr cleanup - compact handoff (2026-09-14)

The original pond#237 comparison is complete and preserved under
`docs/evals/2609-14-pond-237/`. Reviewer C found the decisive blocking defect but
failed to deliver its report after two runs exposed disk/quarantine and checkout-root
sandbox problems. Its nine-task lane included five production lenses, two in-band
model-comparison shadows, a manager and a report writer.

## Current production shape

WP1-WP4 in `docs/plans/2609-14-review-pr-proof-cost.md` are complete.

- Production `/review-pr` has seven roles: manager, five lenses and report writer.
- Model comparisons are out-of-band Pond experiments. No shadow role, route, prompt,
  dependency or report rule exists in the production skill.
- `review-seed.yaml` is the sole production model-routing authority. Model IDs never
  enter manager, lens or report prompts.
- The goal contains the five lens-specific sections. Shared finding rules live in the
  byte-identical lens role prompts; report schema lives in the report-writer prompt;
  task creation lives in the manager prompt.
- Templates are snapshotted outside the reviewed checkout and symlinked into
  `.bernstein/templates`, keeping Bernstein RAG from reinjecting them.
- Pi, Claude and Codex PATH wrappers suppress measured operator/project prompt and
  persistence surfaces. Codex project-local `.codex/config.toml`, its product baseline
  and host skill catalogue remain unclosed, so the public-repository fence remains.
- Repository developer commands now live in the root `Justfile`; use `just check`,
  `just operator-check`, `just test`, `just eval` and `just ship` from the root.

## Proof and commits

- `e45ffbb` - isolate measurement from production reviews and flatten the Justfile.
- `90929c4` - clean stale KB current/history boundaries.
- `f9c42e5` - final corpus row: 4/4 RECOVERED, zero failed tasks, zero surviving
  processes, clean revision `90929c4`, jobs=4.
- `b3284f9` - align AGENTS and KB rule-carrier documentation with the prompt split.

The model-free operator suite passed 108 tests and `just check` passed after the
implementation and KB integration.

## Next action requested by the operator

After release, rerun pond#237 against the frozen historical head
`1c32a5888ab096bbb00f4bd0830777fc5be70e87` and its original base. Preserve Reviewer C
unchanged; record the new run as Reviewer D. It should use only the seven production
roles. Any alternative-model comparison happens afterward, outside the review.
