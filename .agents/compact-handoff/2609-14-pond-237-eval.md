# pond#237 lane evaluation - compact handoff (2026-09-14)

Current handoff. `2609-11-review-pr-path-a.md` describes work that is finished and
shipped; read it only for history.

## The plan is not in this file

It is committed in the repository, as a three-document bundle:

- **`docs/evals/2609-14-pond-237/README.md`** - the comparison index: the PR, the
  three reviewers, why A returned 28 findings and B returned 6, and the grading
  rules, all fixed before reviewer C runs.
- **`docs/evals/2609-14-pond-237/plan-review-pr-run.md`** - the executable plan:
  the 11-agent lane table, the five steps, and every hard-won engine fact. It is
  written to be picked up cold and is the source of truth for the run.
- **`docs/evals/2609-14-pond-237/baseline-polish-opus-5-high.md`** and
  **`baseline-polish-codex-5.6-sol-high.md`** - the two `/polish` baselines,
  captured before the run.

Read `plan-review-pr-run.md` first. This file deliberately keeps no second copy of
the plan, so the two cannot drift.

## State: steps 1 and 2 are done; the corpus proof is the gate

The lane was rebuilt on 2026-09-14: nine custom roles, one per lens, with the
engine's own role vocabulary kept out of every agent's context, and the codex
reasoning effort pinned by a PATH shim rather than by the host's config file.
`claude-sonnet-5` left the lane entirely.

```
01c6f98  fix(build-run): the codex effort gate was right for the wrong reason
0352f6f  feat(review-pr): the codex shim pins reasoning effort to the run
9dd5038  fix(review-pr): a role template per role, not just the manager
aafffe3  feat(review-pr)!: one custom role per lens, and no built-in roles in context
f2752c8  docs(evals): the pond#237 three-reviewer comparison bundle
```

Corpus evidence so far, both on `case-01-off-by-one`:

- **Fast lane** (gemini manager, qwen lenses): MISFILED in **695s**, the fastest
  path-A run on record, 0 ERROR. The planted defect WAS found as `correctness` in
  the right window with a measured repro table; it missed `must_mention` only by
  never spelling `calculate_total_pages`, so the scorer matched a different
  finding and returned MISFILED. A scorer artifact, not a review failure - and the
  case stays frozen regardless.
- **Real lane** (codex manager and lenses, qwen + gemini on 4 and 5): in flight.

The standing bar is RECOVERED on the smoke case before pond#237 runs. Cases 02-04
then run alongside the pond run at `--jobs 1`.

Two upstream questions were settled and need no re-asking: upstream bernstein main
is byte-identical on every file that governs roles, prompts and task creation
(29 commits ahead, same version 3.19.2, all six local patches still apply and
none became unnecessary), so **stay pinned**; and `~/.codex/config.toml` is still
at `high` - the shim makes that unnecessary for /review-pr, but /build-run builds
no shim and its readiness gate still requires it.

## Standing session rules

- ADHD response mode is active and persists until the operator says "stop adhd
  mode" or "normal mode".
- Never read or print the API key in `~/.pi/agent/models.json`, or any secret
  value, into the transcript.
- The `/review-pr` fence is public repositories and this repository's eval corpus
  only, with no GitHub token ever handed to a model session. pond is public, so it
  is inside the fence.
- Two operator follow-ups were raised and closed as "do not raise again". Do not
  raise them again.
