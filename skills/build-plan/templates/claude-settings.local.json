{
  "permissions": {
    "allow": [
      "Bash(bernstein:*)",
      "Bash(uv tool install:*)",
      "Bash(lsof:*)",
      "Bash(pgrep:*)",
      "Bash(kill:*)",
      "Bash(git worktree:*)",
      "Bash(git reset:*)",
      "Bash(git checkout:*)"
    ]
  },
  "autoMode": {
    "allow": [
      "$defaults",
      "This checkout is a build workspace. Routine authorized work includes running the installed build-plan, build-run and build-close Python scripts; configuring the installed scorer and verified engine source; inspecting the identified run server and process receipts; and creating or removing workflow-owned worktrees. Process termination must match recorded PID and creation time. Preserve native journals, scorer attempts and judge receipts before cleanup. The driver ends with a local validated branch. Publishing and merging use existing user authorization or a final explicit decision."
    ]
  }
}
