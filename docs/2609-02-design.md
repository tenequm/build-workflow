# build-workflow design (2026-09-02)

Decisions taken on 2026-09-02 after the executor evals of 2026-08-31 / 09-01
(Codex gpt-5.6-sol, Opus 5 / 4.8 / 4.6, Gemini 3.7 Flash via agy; records in
`personal/docs/codex-subagents-herd-eval/`) and a survey of orchestration tools.
Target metric: wall time from zero to a merged, judged result. Cost and quota
are not constraints.

## What the day was made of

gopost v3 build, 2026-09-01, 12.5 h wall:

| block | wall | what set its length |
|---|---|---|
| design pass, one Fable session | 3h50 | sequential thinking |
| build chain 0b..10 | 3h33 | dependency graph; 210 of 213 min were executor time on the chain |
| polish and hotfix tail | 3h47 | defects merged in the first pass, found afterwards |

Executor choice moves only the middle block, by the per-phase delta. Flash is
0.55x Codex wall with Codex-level defect counts and worse completeness; Opus 5
runs at Codex wall with a fraction of the defects (2-4 vs 12-18 certain over
two judge rounds). The levers are the chain shape, judging in parallel, and the
defect rate of what gets merged.

## Angles adopted

1. Dispatch from a dependency DAG, not from the driver's attention (Bernstein).
2. Speculate past the judge: merge on the scripted gate, start dependents,
   judge beside them, fix as a later node. Expressed in the DAG, no new code.
3. Multiple candidates: Flash as a shadow lane, run as its own plan against the
   same base and archived for later blind judging, never in the chain. Out of v1.
4. Plan for decomposability; critique the plan before launch (readiness gate).
5. Verification as a ratchet with evidence: scripted scorer, blind judge that
   must reproduce a defect, judge family never weaker than the executor.
6. The polish tail as non-blocking `polish-N` nodes owned by the phase's files,
   running beside the next phase. AVO's kernel is not adopted; its invariants
   are (gate separate from objective, one item per accepted candidate, every
   rejection a ledger row, supervisor only after the stall detector).

## Tool choices

- Bernstein: deterministic scheduler, YAML plan with conditional edges and
  retry, worktree per task, janitor + gate pipeline, file state under `.sdd/`,
  gates as entry points. Executors and judges run through Bernstein's own
  adapters (`claude`, `codex`, `agy`); they commit on their branch
  (`agent/<session>`), never push, never main. Completion is Bernstein's
  `git-diff` output mode: the agent commits and the process exits.
- Our package owns only what Bernstein does not do: readiness and pins, the
  scorer (gate command, allowlist, report claims), the judge verdict, the
  ledger and `runs.jsonl`, plans, briefs and skills.
- Not adopted: Claude Code dynamic workflows (harness lock), acpx (second
  transport, no agy), pi-workflows (Pi-bound agent nodes), Gas Town (owns
  tmux), Agent Orchestrator (LLM in the loop, desktop app), a herdr adapter
  (below).

## Why native adapters

The first four stages drove executors through herdr panes behind a custom
Bernstein adapter. A spike on 2026-09-02 (`spike-report.md`, run `smoke-native`,
port 8052) measured the native path instead. Six questions:

| # | Measurement | Yes/No | Evidence |
|---|---|---|---|
| 1 | Nested Claude guard blocks a spawned `claude` | NO, it starts | `CLAUDECODE=1` in the driver; `env_isolation._BASE_ALLOWLIST` drops it, spawned claude ran to a committed review |
| 2 | Codex runs high effort + gpt-5.6-sol | YES | session rollout carries `"reasoning_effort":"high"`, `"model":"gpt-5.6-sol"`; the lock is `~/.codex/config.toml`, off-repo |
| 3 | `tests` gate = our gate, per task, in the worktree, pre-merge | PARTLY | cwd, branch, one `gates/<task>.json` per task and the pre-merge refusal are right; `condition: always` skipped and CLAUDE.md was gone |
| 4 | `mock` adapter drives a step to completion | NO | exited in 3.3 s, 0 tokens, wrote nothing; `mock.py` only patches the Bernstein demo repo |
| 5 | Per-step `cli:` dispatches the right adapter | PARTLY | both stages routed, but through `role_model_policy`; the step `cli:` won only the first spawn and was lost on the retry |
| 6 | Judge sees a non-empty `git diff <base>..HEAD` | NO | the merge advanced the integration ref, so base and HEAD both resolved to the same sha and the judge wrote "the diff is empty" |

### What the herdr path cost

Every one of these is in code that existed only to drive a pane, and none of it
survives in the native path.

- Bugs, by class, over stages 1-4: two writers on one decision (Bernstein's
  injected prompt told the agent to self-complete; a judge did, so its task
  passed unjudged); unresolved path, twice (a relative workdir resolved against
  the herdr server's cwd and opened the judge in an unrelated repo; the plan
  root resolved `--git-common-dir` against the process cwd); trusted input (the
  selector's Claude tier names reached `codex --model sonnet` until model and
  effort were locked per adapter class); missing setup (only agy was
  pre-trusted, so codex and claude spawns returned `agent_not_ready`); race (the
  judge started 7 s before the merge and got the repo root); keyword proxy for a
  verdict (the parser counted "certain" over the whole review and blocked a
  `merge as-is`); language assumption (a Go lint cache clean on every repo, "no
  lint result" on linter-less ones); wrong file set (the allowlist check missed
  untracked files and counted orchestrator writes).
- Re-runs and their wall: the gopost 1a replay ran 17m49s to UNHEALTHY with no
  gate row, no merge and no verdict, and the retry was hijacked inside 60 s by
  two orchestrators from the dead run still holding port 8052. The fake-executor
  run 1 died on stale entry-point metadata; a smoke run failed 3/3 because two
  plans in one repo make `load_plan` refuse; one judge spawn was lost to an
  `agent_pane_busy` race and retried; one executor spawn was refused for landing
  at the repo root and burned a retry.
- The completion workaround: `SpawnResult.proc` was the watcher, not the agent,
  so Bernstein's orphan snapshot could rule a live agent dead. The watcher had
  to POST `task complete` and then wait for whichever proof landed first -
  `closed`, three consecutive `done` polls spanning two 3 s ticks, or the
  SIGTERM sent before merging. Settling cost a fixed ~40 s (two 20 s idle polls)
  for a 4 s script.
- Permission stalls: pane agents needed pre-trust, and herdr's dialog guard
  refused a ready codex on any marker word in its output. Native adapters carry
  the flag instead (`claude --permission-mode bypassPermissions`,
  `codex exec --sandbox workspace-write`), so there is no trust step to stall.
- The walkthrough tax: the five skills described panes, workspaces and settling,
  and a driver following them literally hit 12 broken or ambiguous lines.

### The four contradictions the spike found, and their fixes

1. The judge's base ref moves. `git diff <base>..HEAD` with `base` = the
   integration branch is always empty after the direct merge advanced it.
   Fix: `run-config` freezes `git rev-parse <integration branch>` at run start
   and the judge brief diffs that sha (or `HEAD^1..HEAD`).
2. `condition: always` is not always. The built-in `tests` gate returns no
   command when no Python file changed, and the step is reported `skipped`.
   Fix: `command_override: "bernstein-herdr gate"` on the pipeline step, which
   runs verbatim. Bernstein also restores the task CLAUDE.md before the gate, so
   the gate identifies its step from cwd (the agent id) through `team.json` and
   `tasks.jsonl`, never from CLAUDE.md.
3. Per-step `cli:` is not the dispatch key. It won the first spawn and was lost
   on the retry; the durable key is the role, resolved through
   `role_model_policy`. Fix: one role per distinct cli/model; step `cli:` stays
   as documentation.
4. The sidecar brief never reaches the agent. Only the step's `description:`
   does. Fix: the brief goes in the step description, and the brief lives at a
   TRACKED path (`.agents/build/plans/<slug>/<step>.md`) because the run
   directory is untracked and outside every worktree.

One further limit, not a contradiction: a blocked gate is terminal once
`gate_repair_enabled: false` is set in the seed. On Bernstein's default `true`
one `[GATE-REPAIR] <title>` task is posted on the same branch and worktree first
(`core/tasks/task_lifecycle.py:3210-3260`); the template turns it off because a
judge step's block is a verdict, not a repairable failure, and this workflow has
a briefed `fix-N` step for that. Either way the gate itself must archive the
diff and report and write the `runs.jsonl` row on the failing path.

### Role names, and what the seed turns off

The five roles are `resolver` and `ci-fixer` (codex executors - two names
because Bernstein batches concurrently open tasks by role, so parallel codex
steps need distinct roles), `analyst` (claude executor), `adversary` (judge)
and `visionary` (shadow). They are not descriptive names by
choice: they are the names in Bernstein's plan-schema role enum
(`planning/plan_schema.py:22-41`, enforced by `plan validate`) that NO catalog
agent carries and that `_ROLE_AFFINITY` does not reach
(`agents/catalog.py:790-800`), which is the only way to keep the 10.7 KB catalog
persona off the front of every prompt. Measured on the walk-through run of
2026-09-02: with `role: resolver` the spawner log carries
`Loaded 154 Agency specialist(s)` and NO `Catalog agent ... selected for role`
line, and the codex prompt is 7,920 bytes with no catalog section. Residual:
every enum role still resolves to one of Bernstein's own shipped role skill
packs (1-3 KB) -- `resolver` opens "You are a Merge Conflict Resolver" -- which
is the intended baseline, not the catalog. Details:
`docs/2609-02-persona-prefix.md`.

Dropping `reviewer` for `adversary` also drops Bernstein's role tool allowlist
for the judge (`adapters/claude.py:311-316`), so the judge gains Write and Edit
and nothing mechanical stops it editing code: a judge step runs the verdict
path, not the scorer's allowlist check. The judge brief forbids it in words.

Two seed keys are set against their defaults. `evolution_enabled: false`, or
Bernstein self-spawns "Upgrade: Improve task success rate" tasks mid-run that
write into the repo outside every allowlist. `gate_repair_enabled: false`, per
the paragraph above. There is NO key for the warm pool: `warm_pool` is accepted
as a top-level seed key (`seed_parser.py:2322`) and nothing reads it, the pool
is built unconditionally at `orchestrator.py:7067-7072`, and speculative slots
carry `worktree_path=""` (`task_lifecycle.py:288-297`) so a claiming spawn
resolves to the repo ROOT (`spawner_core.py:4471-4479`) and switches the root
checkout to an agent branch. The gate refuses to score at the root, so the step
blocks instead of merging garbage; the driver's recovery is
`git checkout <integration branch>`, which is why `git checkout` is in the
permissions template.

### Two constraints on any target repo

- **One plan per root.** The merge target is the branch checked out at the repo
  root, and `load_plan` refuses a repo holding two plans. A second concurrent
  plan needs its own top-level worktree or clone.
- **A real CLAUDE.md.** Bernstein's worktree isolation refuses a symlinked
  `CLAUDE.md` as pointing into parent-repo mutable state, then falls back
  silently to the main workdir on an agent branch, where nothing can ever merge.
  Readiness fails a symlinked root `CLAUDE.md` or `AGENTS.md` up front.

## Patterns copied from pi-workflows / acpx (contracts, not code)

- effects declare recovery before running: idempotent (relaunch a worktree) vs
  manual (a merge); an uncertain crash parks a manual effect as ambiguous
- claims carry a generation; a stale owner cannot write a run row
- workflow source is content-addressed; resume refuses a changed source
  (brief + spec sections + plan node hashed into the run row)
- step contract with attempt ids; the report is a validated submission
- protected human decisions: only a person answers, model tool cannot
- compose by named exits; re-entry starts with empty state
- progress never routes; a repair loop must show changed evidence or stop
- one status projection; renderers never infer state from rows
- judge children: read-only tools, verified model, no silent fallback

## Judge family rule

Claude reviewing Codex raised pass rate 71.6 -> 89.7; Codex reviewing Claude
dropped it 91.4 -> 82.8 (arXiv 2607.21656, 116 LiveCodeBench tasks). The judge
is Fable or Opus 5 for every arm. Codex and Flash never judge Opus.

## Repo layout in a target project

```
docs/spec.md                     tracked; section numbers frozen; label "DESIGN n" kept in citations
.agents/build/plans/<slug>.yaml  tracked
.agents/build/runs/<run>/        untracked: briefs/, contracts/, readiness/, reports/, judge/, shadow/, runs.jsonl, ledger.md, handoff.md
.sdd/                            Bernstein: runtime/, worktrees/<role>-<id> (one per task), traces/, metrics/
```

Skills take the run directory as an argument; there is no `current` pointer. The
run directory name is not free: `plan.run_dir` is `.agents/build/runs/<plan name>`,
so the plan's `name:`, the plan file name and the run dir slug are one string,
chosen in /build-design.

## First test

Replay gopost phases 1a and 1b on the archived base commits (Codex on 1a, Opus
on 1b) through the pipeline and compare rows against
`personal/docs/codex-subagents-herd-eval/runs.jsonl`. Pass when the executor
commits on its branch and the merge lands, the gates catch the 1b harness defect
the judge found, the judge reads a non-empty diff against the frozen base sha,
and the judge node runs while a dummy dependent starts.

### Acceptance outcome (2026-09-03): accepted piecewise, v5 skipped

Four acceptance runs on the gopost 1a+1b case, each dying one engine bug
further along; every criterion was proven across runs A and B of v4, and the
planned fifth run was skipped by decision - the first real build serves as the
single-run proof, producing value instead of a synthetic rerun.

| criterion | proven by |
|---|---|
| 2: gate row + per-attempt archive per step | v4 run B (7 scorer rows, archives keyed per attempt, status.txt written) |
| 3: judge spawns after phase-1's merge, diffs `refs/build/base/<slug>` | v4 run A (also v3: verdict `merge after listed fixes`, 5 certain matching the eval ground truth) |
| 4: fix-N one commit per certain defect, own scoped gate green | v4 run A (three blocks to first-gate green after the plan carried `fixes:` + scoped `gate_cmd`) |
| 5: phase-2 concurrent with judge-1 | v4 run B (both merged, no conflict) |
| 1: every task closes healthy in ONE run | open - v4 run B lost its judges to the tuning-snapshot bug, fixed after; the first real build is the proof |

Wall time for a clean DAG, measured productive segments: phase ~10-17 min,
judge ~12, fix ~12, judge and next phase overlapping - roughly 45-70 min end to
end when nothing is killed.

### Replay checkouts must not share an object store

A replay case is only a measurement if the answer is unreachable from the
checkout the executor runs in. `git worktree add` shares the parent repo's
object store, so every later commit, including the real merged phase, stays
reachable, and the executor can (and did) reproduce the merged commit byte for
byte: the gopost 1a replay on 2026-09-02 produced a tree with an empty
`git diff` against the answer commit, so the run measured nothing about the
executor.

Cut the replay from history instead:

```
git clone --single-branch --no-tags --branch <base-branch> <src> <replay-root>
git -C <replay-root> remote remove origin
```

`--single-branch --no-tags` keeps only that branch's ancestry, and dropping the
remote stops a later fetch from bringing the rest back. Verify with
`git -C <replay-root> log --all --oneline | wc -l` and by confirming the answer
commit is unknown (`git cat-file -e <sha>` fails).

Real builds are unaffected: they run forward from a base that has no answer in
the repo, so a Bernstein worktree over the working repo is the right thing there.

## Cut from v1

acpx, tournaments and automated best-of-N selection, the AVO discover stream,
controllers, discard-and-relaunch speculation, cross-model auto-selection,
agent teams, the shadow lane.

## Package notes (finish)

Measured 2026-09-02 on `scratchpad/smoke-finish` (a copy of the smoke repo, plan
slug `sf`), against Bernstein 3.19.0 at `~/pjv/sipyourdrink-ltd/bernstein`.

### The herdr path is gone from the package

`adapter.py`, `herdr.py`, `watch.py`, `gates/blind_judge.py` and the
`bernstein.adapters` entry points `herdr-claude` / `herdr-codex` / `herdr-agy` /
`herdr-fake` are deleted, and `judge.py` lost `stage_judge` and `judge_worktree`
with them. `stage_judge` was the only reader of the root `templates/` directory,
so the package no longer reaches outside its own tree for a prompt: the judge
reads the brief the plan's `description:` names, in its worktree. What is left is
`ready`, `run-config`, `gate`, `agy-session`, plus the `scorer` gate entry point
(the only surviving `bernstein.gates` name -- `blind_judge` spawned through the
herdr adapter and had nothing left to spawn with).

### Why the gate ran twice, and what fixes it

Not a retry and not `gate_repair`. Two independent call sites run the whole
pipeline on the same worktree, about a second apart, both inside
`_process_single_completed_task`: the pre-merge verification
(`core/tasks/task_lifecycle.py:5025` -> `_run_quality_gates`, `:3148`, through
`_gate_coalescer.run`) and the merge-time refusal check
(`:5041` -> `reap_completed_agent` -> `core/agents/spawner_merge.py:600` ->
`_quality_gate_refusal`, `:442`, calling `run_quality_gates` directly). Site 2
imports `run_quality_gates` itself (`spawner_merge.py:428`) and re-parses the seed
(`:393`), so it bypasses the coalescer -- which only serialises and deliberately
does not reuse results (`core/quality/quality_gate_coalescer.py:17-21`).

`bernstein-herdr gate` now memoises on `(task id, worktree HEAD)` in
`<run>/gate-memo/<task>-<head12>.json`. The second invocation prints the recorded
row and returns the recorded exit code without a new ledger row, a new
`runs.jsonl` row or a new archive. HEAD is in the key so a genuine re-gate after a
repair commit is scored afresh. Measured: one `ledger.md` gate line and one
row pair per task, where stage 7 recorded every one of them twice.

### Warm pool: no seed key, so refuse instead

`warm_pool:` is a valid top-level seed key (`core/config/seed_parser.py:2322`) and
is inert: `load_warm_pool_config` (`core/agents/warm_pool.py:253`) has no
production caller. The pool is built unconditionally
(`core/orchestration/orchestrator.py:7069`) with
`max_slots=max(1, min(2, seed.max_agents))`, which is never 0, and is consulted at
`core/agents/spawner_core.py:4472`. The root spawn is a Bernstein bug, not a
setting: the speculative filler creates slots with `worktree_path=""`
(`core/tasks/task_lifecycle.py:291-297`) and `spawner_core.py:4475` does
`Path("")` -> `Path(".")` -> the orchestrator's cwd, the repo root.

So the package refuses at both ends instead. `gate` blocks when cwd is the repo
root and names the branch the root was left on; `run-config` refuses to start a
run whose root checkout is on `agent/*`, `salvage/*` or `spec/*` and prints
`git -C <root> checkout <base_ref>` to recover. Readiness checks the same thing
before a token is spent.

### Engine patches: the branch the workflow runs on

Bernstein 3.19.0 from PyPI is not usable for this pipeline. The engine is installed
from the clone `~/pjv/sipyourdrink-ltd/bernstein`, branch `fix/warm-pool-empty-worktree`
(backed up at `tenequm/bernstein`), 18 commits on top of 3.19.0, each measured
before it was written and each with tests:

| commit | what 3.19.0 did | measured |
|---|---|---|
| a8eaa3366 | warm-pool slot with `worktree_path=""` spawned the agent at the repo root | 2026-09-02, every judge spawn |
| bdb7d7b6f | worktree-creation fallback, resume and a removed preserved worktree all ran at the root; salvage on a stale plain dir committed `.sdd/` onto the integration branch and renamed it `salvage/<session>` | acceptance v1 |
| 60da6320c | `--port N` never reached agents: prompt and `bernstein task complete` used 8052 | engine smoke 2026-09-03 |
| f99d8d9dd | a session that exited 0 was failed when its log tail contained `401`, `403`, `connection refused` etc. with error context, after its merge landed | same smoke, three retries + DLQ |
| b08b24a17 | orphan verdict read a stale top-of-tick snapshot and reopened a task already done | acceptance v1 post-merge resume |
| 94ab4e0e3 | claude hook URL hard-coded 8052 | 9 hook 401s per judge session |
| 9e2e75185 + b776f72e8 (023ed427c superseded) | a retry gets a NEW task id; the readiness filter built a raw id set, so dependents of a retried task never spawned | acceptance v2 |
| 195431443 | run called itself terminal in the gap between stale-claim fail and retry creation; the stall backstop's predicate could never fire | acceptance v2, 36 min idle |
| 724de1591 | auto-completed a clean exit that left uncommitted work | acceptance v2, 14 files thrown away |
| 3ae487b26 | `codex exec --sandbox workspace-write` cannot write a linked worktree's `.git` (a file pointing outside the root), so no codex agent could commit; now `--add-dir <git-common-dir>` | acceptance v3, all five codex agents |
| 0b060649c | agent runtime ceiling ignored `tuning.orchestrator.max_agent_runtime_s` | acceptance v3, 1800s kills |
| 79c5b7c4c, 12a6cf4c7, a69b89d5f, 4e0eae632 (+ a4db7f2b3 tests) | the tuning-snapshot family: consumer modules bound the frozen defaults singletons at import time, so `tuning:` values reached `config_snapshot.json` and none of the kill paths - liveness grace, escalation SIGTERM tier, watchdog freshness, starting-timeout window all used shipped defaults | acceptance v4, `grace_s=90` SIGTERM of healthy judges at 125s while grace 600 was configured |

The kill-adjacent surface (agent_lifecycle, heartbeat, heartbeat_escalation,
spawner_core, task_lifecycle, models, watchdog) is verified free of stale
snapshots as of 2026-09-03; ~33 singleton-import sites remain in non-killing
modules, closable permanently only by a lint rule banning the import form
outside `defaults.py`.

Upstream status (2026-09-03): the first two guards MERGED as
[#5344](https://github.com/sipyourdrink-ltd/bernstein/pull/5344), shipped in
v3.19.1. Open: [#5375](https://github.com/sipyourdrink-ltd/bernstein/pull/5375)
(codex `--add-dir`), [#5377](https://github.com/sipyourdrink-ltd/bernstein/pull/5377)
(retry lineage), [#5378](https://github.com/sipyourdrink-ltd/bernstein/pull/5378)
(port propagation), issue
[#5379](https://github.com/sipyourdrink-ltd/bernstein/issues/5379) (watchdog
ceiling design question). Held locally: the lifecycle trio (janitor evidence
gone) and the stall-backstop half of 195431443 (the patch is wrong - the
backstop is only entered when both terminal buckets are empty; the 36-min idle's
true cause is undiagnosed). One known site is left unpatched:
`cli/run_bootstrap.py:1084` posts its teardown nudge to the import-time default
URL; it fires after the run and cannot fail a task.

Two consequences for the driver. The run must be launched with
`BERNSTEIN_SERVER_URL=http://127.0.0.1:<port>` (belt and braces with 60da6320c; the
skill prints the line). And a `salvage/<session>` branch after a claude agent is
expected: Bernstein injects `.claude/settings.local.json` into every claude
worktree, so the agent's own branch is salvaged holding only that file. The
branch-loss signal is the integration branch missing, not a salvage branch existing.

### Evolution and gate repair: the two seed keys to set

`evolution_enabled: false` (`seed_parser.py:2517`, default `true`;
`orchestrator.py:549` builds the `EvolutionCoordinator` only when it is on)
stops the self-spawned `Upgrade: <proposal>` tasks
(`orchestrator_evolve.py:817`) that write into the repo mid-run.
`internal_llm_provider: none` also disables it (`config_schema.py:1017-1020`).

`gate_repair_enabled: false` (`seed_parser.py:2518`, default `true`;
env `BERNSTEIN_GATE_REPAIR` overrides) is what makes a blocked gate terminal.
Left on, a blocked *quality* gate (not cross-model, not rules --
`task_lifecycle.py:5037`) spawns one `[GATE-REPAIR] <title>` task in the SAME
worktree and branch (`_preserved_worktrees`, `:3273`, resumed via
`spawn_for_resume`), with the same role and owned files, whose whole description
is the last 40 lines of the blocking gate's detail plus "Make the existing tests
and lint pass. Do not rewrite the feature. Keep the diff as small as possible."
(`_build_gate_repair_goal`, `:3189-3207`). Exactly one attempt:
`gate_repair_attempted` is pre-seeded on the repair task (`:3251-3259`), and the
original task is failed with `gate_repair_scheduled` (`:5081-5087`).

Recommendation: `false`. The repair task is told to make the gate pass, not to fix
the defect, and it edits the same branch the gate just refused -- which is how a
blocked allowlist violation gets "repaired" by deleting the offending change
rather than by the driver deciding what should have happened. The blocked path is
already fully archived (diff, report, row) by our gate, so the driver has
everything needed to dispatch a real fix brief.

### `fake`: a plumbing executor with no model

`bernstein_herdr.fake:FakeAdapter` is registered as the native adapter `fake`
(51 lines with its docstring). It passes admission: `bernstein adapters check`
reports 0 fail and lists `fake` as `skip / no contract`, exactly like the shipped
`mock`, and `fake` appears in `bernstein run --cli`'s enum. `spawn()` runs a
python snippet in the worktree that resolves the step from the prompt through
`plan.step_from_prompt`, appends a placeholder line to every allowlisted file,
writes the sidecar's report path with every item SKIPPED, and commits.

Measured, one step: 4 s of executor wall (`tasks.jsonl claimed_at` to gate),
~90 s from `bernstein run` to the merge -- the rest is orchestrator bootstrap.
Gate passed, allowlist clean, report present, `files: 1, code_add: 1`.
It is a plumbing test only: it never reads the brief's Items, because a fake
executor that tried to do the work would be a bad model, not a wiring check.
